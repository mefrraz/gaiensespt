# Plano: .gitignore + About + Game Page Redesign

## 1. .gitignore Update

Adicionar ao `.gitignore` atual:

```
# Config
.kilo/
.vercel/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Build
dist/
node_modules/
```

## 2. About Page — Atualizar

### O que mudar:
| Secção | Antes (incorreto/desatualizado) | Depois |
|--------|----------------------------------|--------|
| **"Quando Atualiza?"** | Fala do schedule antigo (GitHub Actions às 12h/19h/23h) | Explica o novo sistema: "Atualizado a cada visita. Dados em cache partilhado por 5 min." |
| **"Fonte dos Dados"** | OK, mantém | + adicionar menção ao proxy Vercel Edge |
| **"Funcionalidades"** | "Adicionar jogos ao calendário" (não implementado) | Remover essa linha |
| **Design** | Glass-card antigo | Usar novo design system (bg zinc-50, slide-up animations) |
| **Back button** | `Link to="/"` (rota não existe) | `Link to="/dashboard"` ou `onClick={() => window.history.back()}` |

### Estrutura final da About:
```
Header com back button
- Intro: Icon + "Gaienses App"
- Criador: André Ferraz, Instagram
- Fonte dos Dados: FPB via Vercel Edge
- Como funciona: Client-side fetch + cache 5min Supabase
- Funcionalidades: (5 itens, sem "adicionar ao calendário")
- Stack: React, Vite, Supabase, Vercel, Tailwind
```

## 3. Game Page — Redesign Completo

### Estado Atual (problemas)
- Spinner genérico no loading
- Layout básico, sem contraste
- Mapa Google Maps iframe pesado e quebra dark mode
- Não há boxscore/stats
- Para jogos agendados: mostra "VS" e "- : -" que é feio
- Volta para "/" que não existe

### Novo Design

**Header:** Back button + "Ficha de Jogo" título + Share button

**Hero Banner:**
```
┌─────────────────────────────────────────┐
│  [VITÓRIA badge verde/ DERROTA badge vermelho]  [escalão]  │
│                                         │
│   [logo]         VS         [logo]      │
│   FC GAIA                OPONENTE       │
│                                         │
│        █████████   █████████            │
│            78    :    65               │
│                                         │
│   🏟️ Pavilhão Municipal de Gaia        │
│   📅 Sábado, 15 Março 2026 · 15:00     │
└─────────────────────────────────────────┘
```

**Para jogos FINALIZADOS:**
- Hero com score grande, badge VITÓRIA/DERROTA
- Secção "Stats" (se disponível via get_game_layer):
  - Pontos, Assistências, Ressaltos (top players)
  - Tabela comparativa (2PM, 3PM, LLM, RO, RD, AS, BP, FC)

**Para jogos AGENDADOS:**
- Hero com "VS", hora visível, badge "AGENDADO"
- Contagem decrescente para o início do jogo
- Botão "Adicionar ao Calendário" (função já existente via ics)

**Info Cards (ambos os modos):**
- Localização (sem iframe, apenas texto + link Google Maps)
- Data e hora formatada

### Novas funções na fpbApi.ts:
- `fetchGameDetails(internalId)` — chama `admin-ajax.php?action=get_game_layer&matchId=...`
- Parse do boxScore HTML

### Ficheiros a modificar:
- `web/.gitignore` — adicionar entradas
- `web/src/pages/About.tsx` — reescrever secções desatualizadas
- `web/src/pages/Game.tsx` — redesign completo
- `web/src/lib/fpbApi.ts` — adicionar fetchGameDetails

---

## Timeline
1. **.gitignore** — 2 min
2. **About page** — 15 min
3. **Game page** — 1-2h
4. **Build + push** — 5 min